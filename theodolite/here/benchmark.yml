apiVersion: theodolite.rocks/v1beta1
kind: benchmark
metadata:
  name: resnet-classifier-benchmark
spec:
  sut:
    resources:
      - configMap:
          name: "resnet-system-configmap"
          files:
            - service-monitor.yaml
            - system-deployment.yaml
            - system-service.yaml
  loadGenerator:
    resources:
      - configMap:
          name: "resnet-system-configmap"
          files:
            - load.yaml
  resourceTypes:
    - typeName: "NodeType"
      patchers:
        - type: "NodeSelectorPatcher"
          resource: "system-deployment.yaml"
          properties:
            variableName: "RN_NODETYPE"
  loadTypes:
    - typeName: "CpuLimit"
      patchers:
        - type: "ResourceLimitPatcher"
          resource: "system-deployment.yaml"
          properties:
            container: "system"
            limitedResource: "cpu"
    - typeName: "ModelSize"
      patchers:
        - type: "EnvVarPatcher"
          resource: "system-deployment.yaml"
          properties:
            container: "system"
            variableName: "RN_MODELSIZE"
  slos:
    - name: "accuracy"
      sloType: "generic"
      prometheusUrl: "http://prometheus-operated:9090"
      offset: 0
      properties:
        externalSloUrl: "http://localhost:8082"
        promQLQuery: avg_over_time(rs_accuracy[15s])
        warmup: 15
    - name: "responseTime"
      sloType: "generic"
      prometheusUrl: "http://prometheus-operated:9090"
      offset: 0
      properties:
        externalSloUrl: "http://localhost:8082"
        promQLQuery: avg_over_time(rs_responseTime[15s]) < 1
        warmup: 15
    - name: "inferenceTime"
      sloType: "generic"
      prometheusUrl: "http://prometheus-operated:9090"
      offset: 0
      properties:
        externalSloUrl: "http://localhost:8082"
        promQLQuery: avg_over_time(rs_inferenceTime[15s]) < 1
        warmup: 15
    - name: "cpu"
      sloType: "generic"
      prometheusUrl: "http://prometheus-operated:9090"
      offset: 0
      properties:
        externalSloUrl: "http://localhost:8082"
        promQLQuery: avg_over_time(rs_cpu[15s]) > 80
        warmup: 15
